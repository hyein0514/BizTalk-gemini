I have updated the `BACKEND_API_URL` in `frontend/js/script.js` to a relative path (`/api/convert`).

This change is necessary for the deployed application to correctly route API calls to the Vercel backend.

Please **re-deploy your project to Vercel** for this change to take effect.
